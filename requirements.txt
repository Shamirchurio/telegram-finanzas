# Dependencias para la función serverless de Vercel (api/index.py).
# El entorno local sigue usando Pipfile / pipenv — este archivo no lo toca.
fastapi>=0.110,<1.0
python-telegram-bot==22.3
libsql-client>=0.3,<1.0
pandas==2.3.1
openpyxl==3.1.5
